function showAge(name, age) {
    return `The age of ${name} is ${age}.`;
}

function showCity(name, city) {
    return `The city of ${name} is ${city}.`;
}

function showHobby(name, hobby) {
    return `The hobby of ${name} is ${hobby}.`;
}

export {
    showAge,
    showCity,
    showHobby
}